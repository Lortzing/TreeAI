{
  "name": "@treeai/event-journal",
  "version": "0.0.0",
  "private": true,
  "description": "EventJournal — append-only TreeAIEvent journal (memory/JSONL), RunState projection with illegal-transition rejection, d2-v1 deep redaction, host-exit recovery convergence, and observability audit records.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:test": "tsc -p tsconfig.build.json",
    "test": "npm run typecheck && npm run build:test && node --test 'dist/tests/*.test.js'"
  },
  "dependencies": {
    "@treeai/contracts": "0.1.0"
  }
}
