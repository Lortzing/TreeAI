{
  "name": "@treeai/runtime-pi",
  "version": "0.0.0",
  "private": true,
  "description": "PiRuntime — Pi SDK session lifecycle (create/restore, prompt, steer, abort, navigateTree, subscribe, dispose). This is the ONLY workspace allowed to depend on @earendil-works/pi-coding-agent; Pi types must not leak to other packages.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run typecheck && node --test tests/unit/*.test.ts tests/real-port/*.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@treeai/contracts": "0.1.0"
  }
}
